version: "3.8"
services:
  customs:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - customs-data:/app/data
    restart: unless-stopped

volumes:
  customs-data:
